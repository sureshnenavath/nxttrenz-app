[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

[redirects]
  # Redirect all requests to index.html for SPA routing (React Router)
  from = "/*"
  to = "/index.html"
  status = 200
